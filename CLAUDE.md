# 골목마켓 (Golmok Market)

동네 기반 중고거래 플랫폼. 취업 포트폴리오 프로젝트.
백엔드 어필 포인트: **실시간 채팅 / 알림 / 동시성 제어(거래·후기·카운터)**

## 스택

- Spring Boot 4.1.1 / Java 21 / Gradle
- MySQL 8.0 (운영), H2 인메모리 (테스트)
- React + Vite — 별도 저장소 [golmok-market-frontend](https://github.com/leejiseong20/golmok-market-frontend). 실제 API와 연동돼 동작한다(목업 없음).

## 작업 규칙

- 모든 응답은 한국어로.
- 코드 조각이 아니라 **완성된 파일 전체**로 제시한다.
- 왜 그렇게 설계했는지 짧게라도 근거를 붙인다. 면접에서 설명할 수 있어야 한다.
- 문제가 보이면 돌려 말하지 말고 직접 지적한다.

## 지켜야 할 제약

- `ddl-auto: validate`. 엔티티를 바꾸면 `golmok_schema_v2.sql`도 함께 고친다.
- `build.gradle`의 Spring Boot 버전은 임의로 변경하지 않는다.
- API 리소스명은 `products`로 통일한다 (`items` 아님).
- 엔티티에 setter를 만들지 않는다. 의미 있는 비즈니스 메서드만 노출한다.
- 연관관계는 전부 `LAZY`. 양방향은 `Product ↔ ProductImage`만.
- 상품은 soft delete. 물리 삭제 금지.
- DB 비밀번호 등 비밀값은 환경변수로만. 커밋 금지.

## 파일

- `API-SPEC.md` — API 명세 (인증/상품/찜/카테고리/동네/검색/거래 일부/채팅)
- `golmok_schema_v2.sql` — 실제 DB 스키마. 이게 기준이다.

## 진행 상황

- [완료] DB 스키마 15개 테이블 설계 및 적용
- [완료] JPA 엔티티 15개 + enum 8개, `validate` 통과
- [완료] H2 테스트 분리, GitHub 푸시
- [완료] API 명세 확정 (에러 포맷 `errors` 필드, 목록별 커서 정렬값 반영)
- [완료] 공통: 전역 예외 핸들러, 에러 응답 포맷(`errors` 필드 포함), 커서 페이징 유틸
- [완료] 인증: 가입 / 로그인 / 재발급(rotation) / 로그아웃 / 중복 확인, Spring Security + JWT
- [완료] 카테고리·동네 조회: 카테고리 전체 / 주소 부분 검색 / 근처 동네 최대 10개
- [완료] 상품 목록·상세 조회: 동네·카테고리·제목/본문 검색, 최신순·낮은가격순 커서 페이징, 이미지·찜·판매자 응답, 조회수 원자적 증가
- [검증] 2026-09-14 `gradlew.bat build` 성공. 기존 129개 + 신규 상품 테스트 66개 = 테스트 195개, 실패 0·오류 0·건너뜀 0. 상품 서비스 28개·MockMvc 37개·동시성 1개 추가. 로그인 목록은 상품 12개/이미지 24개/찜 12개에서 쿼리 3회임을 확인했다. 동시성 테스트는 비로그인·다른 사용자 조회 20건과 본인 조회 5건을 함께 실행해 최종 조회수 20 및 각 증가 응답을 확인했다. 운영 MySQL 실행 및 EXPLAIN은 이번에 수행하지 않았다.
- [검증] 2026-09-16 로컬 MySQL 8.0.44의 기존 `golmok` DB로 앱 기동 및 `ddl-auto: validate` 통과. 같은 SQL 스키마로 생성한 임시 MySQL DB에서 실제 HTTP/API 검증 30개 모두 성공: 가입·중복·로그인·재발급·로그아웃, 한글 주소 검색·근처 조회, 두 정렬의 커서 페이징, LIKE 특수문자, 이미지·찜, 삭제·없는 상품, 입력 오류·위조 토큰, 본인 조회 제외를 확인했다. 동시 상세 조회 12건의 증가분과 각 응답값이 보존됐고, `ON UPDATE`가 있는 실제 MySQL에서도 조회수 증가 시 `updated_at`·`bumped_at`은 유지됐다. 소량 데이터의 가격순 EXPLAIN은 filesort를 확인했으며 운영 규모 성능 검증은 별도다. 임시 앱·DB는 정리했고 기존 DB는 회원 0·상품 0·카테고리 7·동네 3개로 유지했다. `gradlew.bat build`도 성공(UP-TO-DATE, 기존 195개 테스트 결과 재사용). 소스 코드·스키마 수정 없음.
- [완료] 프론트 실제 데이터 연결. 데모 시드(`scripts/seed_demo_data.sql`)로 목록·정렬·검색·상세·조회수·찜을 화면에서 확인
- [완료] 찜하기·찜 해제 API와 마이페이지(내 정보 + 찜 목록, 커서 페이징)
- [완료] 구매내역 조회와 구매확정. 마이페이지에 찜한 상품 / 구매내역 탭 분리
- [완료] 동네 인증: 좌표로 인증(최대 2개), 재인증 횟수, 대표 지정·자동 승격. 로그인 시 대표 동네를 홈에 적용
- [완료] 이미지 업로드(로컬 디스크): 시그니처 검증, UUID 파일명, 날짜별 폴더, `/api/images/**` 서빙
- [완료] 상품 등록·수정·soft delete·상태 변경·끌어올리기·내 판매내역 API. 이미지 경로/실제 파일 검증, 인증 동네 권한 검증, 상품 쓰기 잠금, 프론트 등록/수정/판매 관리 연결.
- [검증] 2026-09-16 인수 기준 `30eb019`의 256개 → 281개(실패 0, 오류 0). `gradlew.bat build` 통과. 고정 Clock의 정확한 24시간 경계, 동시 끌어올리기 1건만 성공, 기존 카운터 보존, 판매완료 후 구매확정 회귀 검증 포함. 로컬 MySQL 8.0.44 validate 기동 및 실제 브라우저 데스크톱·모바일 상품 쓰기 시나리오 통과. 컬럼·SQL 스키마 변경 없음. 커밋·푸시 미실행.
- [완료] 배포 준비: Dockerfile, GitHub Actions(테스트 → GHCR 이미지), `deploy/`(서버 한 대 Docker Compose: MySQL · 앱 · Caddy HTTPS), 데모 시드 스크립트, 단계별 안내(`deploy/README.md`). 프론트 `vercel.json`
- [검증] 2026-09-17 운영 Compose 구성을 로컬 Docker로 실제 기동: 스키마 자동 적용, HTTPS 경유 API, HTTP→HTTPS 리다이렉트, 앱·DB 포트 비노출, CORS 허용/차단, 데모 시드, 이미지 업로드·서빙, 앱 컨테이너 재생성 후 이미지 유지, 운영 로그에 SQL 값 없음. 메모리 앱 290MB(튜닝 전 461MB) · MySQL 160MB · Caddy 15MB, 요청 60회 실패 0
- [완료] 배포 대상을 AWS EC2 에서 Oracle Cloud 상시 무료로 변경. 이미지를 x86·ARM 으로 함께 빌드, 안내서를 Oracle 기준으로 재작성
- [보류] 실제 배포. Oracle Cloud 서버 만들기에서 중단(2026-09-17). 기능 개발을 먼저 하고 나중에 다시 한다. `deploy/README.md`는 Oracle 기준이라 재개할 때 대상에 맞게 고친다.
- [완료] 채팅 1단계(REST): 채팅하기(기존 방 재사용)·목록(안 읽은 수)·방 조회·메시지 조회/전송·읽음·나가기. `chatCount` 원자적 증가
- [검증] 2026-09-17 테스트 281개 → 301개(실패 0). 채팅 API 19개 + 동시 채팅하기 1개. 상품 잠금을 빼면 동시성 테스트가 UNIQUE 위반으로 실패함을 확인했다. 실제 MySQL·프론트 연결은 아직 안 했다.
- [완료] 채팅 2단계(실시간): STOMP over WebSocket(`/api/ws`), CONNECT 시 JWT 인증, 개인 큐(`/user/queue/chat`)로 새 메시지·읽음 이벤트를 커밋 후 전달
- [검증] 2026-09-17 테스트 301개 → 309개(실패 0). 실제 포트로 띄운 STOMP 클라이언트 테스트 8개: 토큰 없음·위조·만료 거부, 다른 출처 거부, 남의 큐 구독·SEND 거부, 두 참여자 수신·제3자 미수신, 같은 사용자 두 연결, 읽음 이벤트. 인터셉터의 구독·SEND 검사를 빼면 2개가 실패함을 확인했다. 실제 브라우저·MySQL 연결은 아직 안 했다.
- [완료] 프론트 채팅 화면(별도 저장소). 실제 MySQL + Vite 프록시로 실시간 수신·읽음·재연결 확인
- (해결) 채팅 도입 후 `scripts/clean_demo_data.sql`이 채팅방 외래키로 실패할 수 있던 문제: 데모 회원이 낀 채팅방을 거래 다음에 지운다(메시지는 CASCADE). 로컬 MySQL 에서 롤백 트랜잭션으로 실행해 확인했다.
- [완료] 채팅방 직거래: 판매자 예약·거래완료, 양쪽 예약 취소, 시스템 메시지, 진행 중 거래가 있으면 상품 상태 변경·삭제 차단. 프론트 채팅방 거래 줄 연결
- [검증] 2026-09-17 테스트 309개 → 321개(실패 0). 직거래 API 11개 + 두 방 동시 예약 1개. 상품 잠금을 빼면 동시성 테스트가 실패함을 확인했다. 실제 MySQL + Vite 프록시로 예약·구매자 예약 거부·시스템 메시지 실시간 수신·상태 변경 차단·취소·재예약·거래완료·구매내역 반영까지 확인했다(확인용 상품은 soft delete, demo4 구매내역에는 기록이 남는다).
- [완료] 리뷰·매너온도: 거래당 양쪽 1회 작성, 받은 후기 공개 커서 조회, 공개 프로필, 채팅 `tradeActions.review`·구매내역 `canReview`, 프론트 작성·프로필·받은 후기 연결.
- [검증] 2026-09-17 인수 기준 `979548a`의 321개에서 리뷰 서비스·API·동시성 33개를 추가했다. 잠금 제거 시 중복 요청이 UNIQUE 위반으로 실패, 원자적 UPDATE를 메모리 계산으로 바꾸면 증가분 유실, `mannerTemp.updatable=false`를 제거하면 로그인 갱신이 온도를 덮는 실패를 각각 확인하고 복구했다. 실제 MySQL 8.0.44 validate 및 데스크톱·모바일 브라우저 후기 흐름을 검증했다. 최종 빌드·테스트 결과는 아래 리뷰 검증 기록 참조.
- [완료] 알림: 찜·가격 인하·예약·예약 취소·거래완료·후기 수신 알림, 알림함 커서 목록·안 읽은 수·읽음·모두 읽음, 기존 WebSocket 개인 큐로 실시간 전달. 프론트 헤더 알림 버튼·알림함 연결
- [검증] 2026-09-17 테스트 354개 → 366개(실패 0). 알림 API 6개, 생성 흐름 5개(찜 합치기·가격 인하·거래·후기·롤백 시 없음·저장 실패해도 원래 동작 성공), WebSocket 알림 전달 1개. 커밋 후 리스너를 트랜잭션 내 리스너로 바꾸면 롤백 테스트가, 찜 합치기를 없애면 합치기 테스트가 실패함을 확인했다. 리스너의 try/catch 는 지워도 테스트가 통과한다(Spring 이 커밋 후 리스너 예외를 삼킨다). 실제 MySQL + Vite 프록시로 데모 계정 알림 전 흐름 확인(확인용 상품은 soft delete, 데모 계정 알림함·거래·후기 기록과 demo1 매너온도 +0.2 는 남는다). 로그인 상태 화면은 사용자 확인 필요.
- (해결) 알림 도입 후 `clean_demo_data.sql`이 `fk_notifications_user`로 실패할 문제: 데모 회원이 받은 알림을 회원보다 먼저 지운다. 롤백 트랜잭션으로 확인했다.
- [제외] 결제(2026-09-17 결정). 택배 거래에만 필요한 기능이라 범위에서 뺐다. 코드·스키마의 결제 흔적은 남긴다(아래 설계 결정 참고).
- [완료] 프론트 라우터 도입(프론트 저장소, 백엔드 변경 없음)
- [완료] 프로필 수정: `PATCH /api/users/me` 닉네임·사진 변경. 프론트 마이페이지 프로필 수정 창, 이웃 프로필 사진 표시
- [검증] 2026-09-17 테스트 366개 → 379개(실패 0). API 12개(성공·공백 제거·같은 닉네임·사진 삭제·중복 409·길이 400·잘못된 사진 400·비로그인 401), 같은 닉네임 동시 변경 1개. UNIQUE 위반을 409 로 바꾸는 처리를 빼면 동시성 테스트가 실패함을 확인했다. 실제 MySQL + Vite 프록시로 demo4 변경·공개 프로필 반영·중복·길이·외부 사진 거부 확인 후 원래 닉네임·사진으로 복구했다.
- [완료] 채팅 상대(`opponent`)·후기 작성자(`reviewer`) 응답에 `profileImageUrl` 추가(상품 상세 판매자는 이미 있었다). 조회가 이미 회원을 fetch join 해 추가 쿼리 없음. 프론트 채팅방·채팅 목록·상품 상세·후기 목록에 사진 표시
- [검증] 2026-09-17 테스트 379개 → 380개(실패 0). 채팅방·목록 사진 경로 테스트 1개 추가, 기존 채팅하기·후기 테스트에 사진 경로 검증 추가. 실제 MySQL 로 채팅 목록·채팅방·받은 후기·상품 상세의 사진 경로 확인(demo1·demo4 에 확인용 사진을 넣어 둠, 복구는 `codex-handoff/scripts/avatar-live-check.mjs` 를 MODE=restore 로 실행).
- [완료] 인기 검색어·검색 로그: 상품 검색 첫 페이지만 비동기(전용 실행기)로 로그 저장, 검색어 정규화, 최근 24시간 TOP 5 집계 + 1분 메모리 캐시(갱신 한 스레드). 프론트 PC 안내 패널 순위·모바일 칩
- [검증] 2026-09-17 테스트 380개 → 391개(실패 0). 정규화 3개, 집계·캐시 5개(24시간 경계·동점 순서·5개 제한·빈 결과·1분 캐시, 시계를 움직여 확인), 비동기 적재 흐름 3개(실행기가 끝날 때까지 기다려 "기록 안 함"까지 확인). 첫 페이지 조건·캐시를 각각 빼면 해당 테스트가 실패함을 확인했다. 실제 MySQL: 식탁 변형 4건이 하나로 합쳐지고 커서 요청은 기록되지 않음, 1분 뒤 인기 검색어 반영, 브라우저 PC 패널·모바일 칩·클릭 검색 확인. 로컬 DB 에 확인용 검색 로그(식탁·ipad·의자)가 남았다.
- [완료] 오래된 데이터 정리 배치: 매일 새벽 4시(한국 시간) 검색 로그 7일·읽은 알림 30일·안 읽은 알림 90일 지난 행을 1,000건씩 별도 트랜잭션으로 삭제. 보관 기간·시각·배치 크기는 `app.cleanup.*` 설정값, 끄기는 `SCHEDULING_ENABLED=false`
- [검증] 2026-09-17 테스트 391개 → 397개(실패 0). 정리 5개(7일 경계·읽음/안 읽음 기준·배치 나눠 끝까지·지울 것 없음·잘못된 설정 차단), 예약 등록 1개(cron "0 0 4 * * *"). 반복 삭제를 한 번으로 줄이면 테스트가 실패함을 확인했다. 실제 MySQL 기동 정상, 로컬 DB 는 보관 기간 지난 행 0건이라 정리 작업은 직접 실행하지 않았다. EXPLAIN 으로 검색 로그 조회의 ORDER BY id 가 filesort 를 일으켜 정렬을 뺐다.
- [다음] 미정

## 알려진 과제

- (해결) `Review.created_at`의 DB 기본값 의존과 일반 예외: `BaseCreatedTimeEntity` 상속과 `BusinessException(ErrorCode)`로 변경했다. 아래 기존 4개 엔티티 과제 중 Review·Notification은 해결, SearchLog·Payment는 남아 있다.
- (해결) 리뷰 도입 후 `scripts/clean_demo_data.sql`이 `fk_reviews_trade` 외래키로 실패하던 문제: 데모 회원이 낀 거래의 후기를 거래보다 먼저 지운다. 로컬 MySQL 에서 롤백 트랜잭션으로 실행해 확인했다(실계정 유지). 이미 반영된 매너온도는 되돌리지 않는다(현재 실계정이 받은·남긴 후기 0건).
- (해결) 후기 동시 수신의 온도 유실·로그인 갱신의 온도 덮어쓰기: DB 원자적 UPDATE와 `User.mannerTemp.updatable=false`로 보호한다.
- (해결) 실제 MySQL에서 후기 중복 조회의 gap lock과 사용자 잠금이 교착했다. 리뷰 작성 트랜잭션만 READ_COMMITTED로 변경하고 중복은 상품·거래 잠금 아래 일반 조회한다. H2로만 확인하면 발견하지 못하는 문제다.
- 후기 작성 기한은 없지만 판매자가 나간 채팅방의 완료 거래에 다시 접근하는 별도 판매 거래내역 화면은 없다. 현재 판매내역은 상품 목록이므로 판매자 작성 진입점은 남아 있는 채팅방이다.
- 후기는 즉시 공개·수정/삭제 불가다. 보복성 평가 완화, 신고·운영자 조치, 지연 공개는 후속 정책이다. 상·하한을 적용하므로 수정/삭제를 추가할 때 단순 역산하지 말 것.
- 후기는 실시간 이벤트를 보내지 않는다. 상대가 쓴 뒤 이미 열려 있는 프로필·매너온도는 화면을 다시 열거나 새로고침해야 반영된다.
- 실제 서버 검증의 `review-*@golmok.test` 계정·거래·후기·업로드 사진이 남는다. 생성한 상품만 해당 실행 ID로 soft delete했다. 사용자 실계정과 기존 데모 데이터는 삭제하지 않았다.

- (해결) 판매자가 먼저 상품을 SOLD로 바꾸면 구매확정이 실패하던 문제: 상품이 이미 SOLD인 경우 상품 전이만 생략하고 거래 확정을 수행한다.
- 업로드 파일의 소유자 메타데이터가 없어 다른 사용자가 알고 있는 서버 이미지 URL을 재사용하는 것까지 막지는 못한다. 이번 검증은 경로 안전성과 실제 파일 존재까지다.
- 데모 사진은 외부 picsum URL이다. 조회는 유지하지만 상품 수정 시 서버에 새로 올린 사진으로 교체해야 한다.
- 실제 브라우저 검증이 만든 `write-<실행식별자>@golmok.test` 계정과 업로드 파일은 남는다. 검증 상품은 해당 실행의 ID만 soft delete했다. 사용자 실계정과 기존 데모 상품은 수정하지 않았다. 전체 데모 정리 SQL은 실행하지 않았다.

- 엔티티 1개(`Payment`)의 `created_at`이 `insertable = false`(DB 기본값 의존)다(결제는 범위 제외). (`Review`, `Notification`, `SearchLog` 는 해결) H2 테스트에서 INSERT가 실패하고, 저장 직후 `createdAt`이 null이다. 각 도메인 구현 시 `BaseCreatedTimeEntity`로 바꾼다. (`RefreshToken`, `Region`, `ProductImage`, `Favorite`, `ChatRoom`, `ChatMessage`는 완료)
- 엔티티가 비즈니스 규칙 위반 시 `IllegalArgumentException`/`IllegalStateException`을 던진다. 403이어야 할 것(참여자 아님 등)도 400이 된다. 각 도메인 구현 시 `BusinessException`으로 바꾼다.
- 동네 인증에 **반경 제한이 없다.** 지금은 어디서 눌러도 가장 가까운 동네로 인증된다. `regions`에 동네가 3개뿐이라 반경을 걸면 시연이 불가능하기 때문이다. 동네 데이터를 실제로 채울 때(행정동 전체 적재) 반경 제한과 인증 횟수 제한(하루 1회 등)을 함께 도입한다.
- `JwtAuthenticationFilter`는 `Bearer `로 시작하지 않는 Authorization 헤더를 무시한다. 형식 오류 토큰을 401로 처리한다는 명세와 어긋날 여지가 있어 별도 정책 정리가 필요하다. 이번 조회 작업에서는 변경하지 않았다.
- 동네 주소 부분 검색은 전체 주소를 연결한 LIKE 검색이므로 일반 인덱스로 검색 비용을 줄이기 어렵다. 근처 조회의 `(lat, lng)` 인덱스 역시 위도 범위 이후 경도까지 탐색에 효율적으로 쓰인다고 보장할 수 없다. 데이터 확대 시 MySQL EXPLAIN과 실측으로 확인한다.
- 상품 검색은 현재 LIKE이므로 스키마의 FULLTEXT ngram 인덱스를 사용하지 않는다. 상품 정렬 인덱스는 `status`가 정렬 컬럼보다 앞에 있는데 목록은 모든 상태를 포함하므로 filesort 회피를 보장할 수 없다. 운영 MySQL EXPLAIN·실측 후 최적화한다.
- 이미지 저장소가 **서버 디스크**다. 운영 Compose는 Docker 볼륨에 두어 컨테이너 교체에는 남지만, 서버 자체를 지우면 사라진다. 오브젝트 스토리지 이전은 후속 과제. 상품에 연결되지 않은 고아 파일 정리 배치도 없다.
- (해결) 상품 읽기와 찜만 구현했던 과제: 이미지 업로드는 `30eb019`에서 완료됐지만 이 항목이 갱신되지 않았었다. 이번에 내 판매내역(`/api/products/me`)과 상품 쓰기 API도 구현했다. 검색 로그 적재는 2026-09-17 구현했다. 조회수가 DB `INT` 범위를 넘는 규모라면 컬럼 확장을 검토한다.
- (해결) 스키마 SQL의 UNIQUE 제약을 엔티티에도 모두 선언했다. 누락되면 `SchemaConstraintTest`가 빌드에서 잡는다. 새 테이블을 추가할 때 `@Table(uniqueConstraints = ...)`를 빠뜨리지 말 것.
- (해결) 마이페이지 판매내역 탭 미구현: 판매내역·상태 필터를 연결했다. 프로필 수정(`PATCH /api/users/me`)은 아직 미구현이다.
- (해결) 거래를 만드는 API 가 없던 문제: 채팅방 직거래(예약·거래완료·취소)로 만든다. 결제·환불과 택배 거래(PAID·SHIPPING 진입)는 범위에서 제외했다(2026-09-17).
- 판매자가 채팅 없이 구매자를 고르는 "거래완료 → 구매자 선택"은 없다. 거래는 채팅방에서만 만들어진다.
- 채팅 테스트의 WebSocket 정리 단계가 서버가 이미 끊은 세션에 DISCONNECT 를 보내 가끔 실패했다(isConnected 가 잠깐 true). 정리 실패는 무시하도록 고쳤다.
- (해결) `Review`의 `IllegalArgumentException`/`IllegalStateException`을 `BusinessException`으로 바꿨다. (`Product`, `Trade`, `ChatRoom`, `ChatMessage`도 완료)
- 채팅방을 나갔다가 상대 메시지로 다시 나타나면 **나가기 전 대화도 보인다.** 스키마에 나간 시점이 없고 `buyer_left`/`seller_left` 불리언뿐이라서다. 당근마켓처럼 이전 대화를 가리려면 `buyer_left_at`·`seller_left_at` 컬럼이 필요하다(스키마 변경).
- 운영 MySQL 의 `last_message_at`은 `DATETIME`(초 단위)이라 **같은 초에 메시지가 온 방끼리는 방 id 순**으로 정렬된다. 채팅 목록에서 거의 동시에 온 두 방의 순서가 실제 수신 순서와 다를 수 있다. 정확히 하려면 `DATETIME(6)`으로 바꿔야 한다(스키마 변경).
- 채팅 목록 쿼리는 구매자·판매자 조건이 OR 라 인덱스 하나로 끝나지 않는다. 방이 많아지면 MySQL EXPLAIN 으로 확인하고, 필요하면 두 쿼리로 나눠 병합한다.
- 실시간 전달은 **메모리 브로커라 서버 한 대에서만** 동작한다. 서버를 늘리면 Redis Pub/Sub 이나 외부 STOMP 브로커 릴레이가 필요하다.
- 실시간 전달은 최선 노력이다. 연결이 끊긴 동안의 이벤트는 다시 보내지 않으므로 프론트가 재연결 시 REST 로 다시 불러와야 한다.
- WebSocket 연결 중 access token 이 만료돼도 연결은 유지된다. 로그아웃해도 이미 맺은 연결은 끊지 않는다(연결을 끊으려면 사용자별 세션 추적이 필요하다).
- 닉네임 변경 횟수 제한이 없다. 당근마켓처럼 "30일에 한 번"으로 막으려면 마지막 변경 시각 컬럼이 필요하다(스키마 변경).
- (해결) 채팅 상대·후기 작성자 응답에 프로필 사진 경로가 없던 문제: 필드를 추가했다. 상품 목록 카드·구매내역 카드의 판매자 사진은 카드가 복잡해져 넣지 않았다.
- (해결) 검색 로그가 계속 쌓이던 문제: 정리 배치가 7일 지난 로그를 지운다.
- 인기 검색어는 같은 사람이 같은 검색어를 반복 검색해 순위를 올리는 것을 막지 않는다. 비로그인 검색은 사람을 구분할 정보가 없다.
- 인기 검색어 캐시는 서버 메모리라 서버를 늘리면 서버마다 따로 기억한다(서로 최대 1분 어긋날 수 있다).
- (해결) 알림이 계속 쌓이던 문제: 정리 배치가 읽은 알림 30일·안 읽은 알림 90일 지나면 지운다.
- 알림 정리 조회는 인덱스 `(user_id, is_read, created_at)` 를 못 쓰고 기본키를 처음부터 훑는다(EXPLAIN 확인). 오래된 행이 id 앞쪽이라 지금 규모는 괜찮지만, "더 없음"을 확인하는 마지막 조회는 테이블 끝까지 볼 수 있다. 커지면 `created_at` 인덱스 추가(스키마 변경)를 검토한다.
- 정리 배치는 서버 한 대 기준이다. 여러 대면 같은 시각에 모두 돈다(이미 지운 행은 다시 지워지지 않아 결과는 틀어지지 않지만 중복 작업). 분산 잠금(ShedLock 등)은 서버를 늘릴 때 검토한다.
- 가격 인하 알림은 **찜한 사람 수만큼 한 건씩 저장**하고, 이 작업이 판매자의 수정 요청 스레드에서(커밋 뒤) 돈다. 찜이 아주 많은 상품은 수정 응답이 느려질 수 있다. 규모가 커지면 비동기 실행·일괄 INSERT 로 바꾼다.
- 찜 알림 합치기는 "안 읽은 알림 있음" 확인 후 저장이라, 두 사람이 거의 동시에 찜하면 안 읽은 찜 알림이 두 개 생길 수 있다. 알림 중복일 뿐 데이터 무결성 문제는 아니라 잠금을 두지 않았다.
- 알림은 앱이 열려 있을 때만 실시간으로 받는다. 브라우저 푸시(탭을 닫아도 오는 알림)는 없다.
- 채팅의 이미지 메시지(`IMAGE`)는 엔티티만 있고 API가 없다. (해결) 시스템 메시지(`SYSTEM`)는 직거래 예약·취소·완료에서 생성하며 이전 과제 기록이 갱신되지 않았었다.
- 운영은 **서버 한 대**(단일 장애점)이고 DB 백업이 수동이다. 이미지는 서버 디스크 볼륨이라 서버를 지우면 사라진다(오브젝트 스토리지 이전은 후속). 상시 무료 서버는 사용률이 낮으면 회수될 수 있어 DB 백업을 정기적으로 받아야 한다.
- 운영 데모 계정 비밀번호가 공개돼 누구나 데모 상품을 고치거나 지울 수 있다. 망가지면 `clean_demo_data.sql` 후 `deploy/seed-demo.sh`로 다시 넣는다.
- 실행 시 `DB_PASSWORD`, `JWT_SECRET`(Base64, 256비트 이상) 필요. 운영은 `deploy/.env`로 주입하고 시간대는 Dockerfile 에서 `Asia/Seoul`로 고정한다.
  로컬은 `~/.gradle/gradle.properties`(저장소 밖)에 `jwtSecret`, 필요하면 `dbPassword`를 두면 `./gradlew bootRun` 만으로 뜬다.
  bootRun 이 띄우는 JVM 은 Gradle 데몬 환경을 물려받아 셸의 `export` 가 닿지 않기 때문에, build.gradle 에서 속성을 환경변수로 넘긴다.
  `JWT_SECRET` 이 실행할 때마다 바뀌면 이전 access token 이 전부 무효가 되므로 로컬에서도 고정값을 쓴다.

## 설계 결정 기록

- **2026-09-17 결제를 범위에서 제외하고, 결제 흔적은 코드·스키마에 남긴다.** 결제는 택배 거래에만 필요한데, 동네 직거래가 핵심 흐름이다. `payments` 테이블·`Payment` 엔티티, 거래 상태 `PAID`·`SHIPPING`, 구매자 구매확정 API 는 삭제하지 않았다. 지우면 스키마·시드·테스트가 연쇄로 바뀌고, 남겨두면 "결제·택배는 이 구조로 붙인다"는 확장 설계로 설명할 수 있다. 현재 `PAID`·`SHIPPING` 거래는 데모 시드로만 생기고, 상품의 `tradeType=DELIVERY`(택배거래)를 골라도 거래는 채팅방 직거래 흐름(예약 → 판매자 거래완료)으로 진행된다.
- **2026-09-17 리뷰 정책**: 거래 기준 POST `/api/trades/{tradeId}/reviews`, CONFIRMED 당사자 각 1회, 기한 없음, 즉시 공개·온도 반영, 수정·삭제 없음. 평가 대상은 서버가 정한다. 삭제 상품·나간 방도 거래 자격은 유지한다. 평점 1~5, 본문 선택 500자, 3점 기준 점당 0.2℃, 범위 0.0~99.9℃. 스키마 변경 없음.
- **Review.write의 온도 부수효과를 제거했다.** 기존 메모리 수정은 동시 요청의 증가분이 사라질 수 있어 서비스가 리뷰 INSERT와 DB 원자적 온도 UPDATE를 같은 트랜잭션으로 실행한다. User의 일반 UPDATE에서 온도를 제외해 로그인·프로필 변경도 오래된 온도를 덮어쓰지 못한다. delta는 double 없이 BigDecimal로 계산한다. 온도 갱신은 프로필 수정 시각을 바꾸지 않는다.
- **리뷰 작성의 격리 수준은 READ_COMMITTED다.** 처음 제안한 REPEATABLE_READ + 후기 잠금 조회는 실제 MySQL의 없는 행에 대한 gap lock과 사용자 잠금이 교착해 변경했다. 상품→거래→사용자 ID 오름차순으로 잠그고, 중복 후기는 상품·거래 잠금 아래 일반 조회한다. READ_COMMITTED라 먼저 커밋된 후기를 새 조회에서 볼 수 있다. 전역 격리 수준은 바꾸지 않는다. `(trade_id, reviewer_id)` UNIQUE는 최종 방어선이다.
- **두 참여자를 INSERT 전에 ID 오름차순으로 잠근다.** 서로 다른 거래에서 상호 평가할 때 reviewer/reviewee FK 공유 잠금을 온도 갱신용 쓰기 잠금으로 승격하면서 생길 수 있는 교착을 피한다. 온도 UPDATE 자체도 원자적으로 구현한다.
- **거래 잠금 쿼리의 fetch join을 제거했다.** 기존 `findWithProduct`의 fetch join은 불필요한 연관 행까지 잠글 수 있어 저장소의 잠금 규칙에 맞춰 분리했고, 동작에 맞게 `findByIdForUpdate`로 이름을 바꿨다. 상품은 이미 먼저 잠근 엔티티를 재사용한다.
- **공개 프로필과 받은 후기는 분리한다.** 프로필은 명세 2절의 요약(삭제되지 않은 모든 상태 상품 수·받은 후기 수), 후기 본문은 생성 시각·ID 내림차순 커서 목록이다. 거래 번호·금액·이메일·채팅방은 공개 후기 응답에 넣지 않는다. 구매내역 작성 여부는 한 페이지를 일괄 조회한다.
- **후기 시스템 메시지는 만들지 않는다.** 후기 때문에 나간 방을 다시 나타나게 하지 않는다. 작성 후 REST 재조회하며 채팅·구매내역의 작성 버튼은 서버가 계산한다.

- **2026-09-16 상품 쓰기 정책**: 등록·수정은 인증한 동네만 허용하고 미인증은 전용 `REGION_NOT_VERIFIED`(403)다. 상세에 `regionId`를 추가했다. Product의 등록 동네 고정 주석은 "대표 동네 변경에 자동 동기화하지 않되, 인증한 동네로 명시적 수정은 허용"으로 변경했다. PUT이 등록과 같은 본문을 받는 명세를 지키기 위해서다.
- **이미지는 경로와 존재를 함께 검증한다.** `/api/images/yyyy/MM/dd/UUID.ext`만 허용하며 실제 경로도 저장 루트 내부인지 확인한다. URL 접두사만으로는 외부 링크·경로 탈출·없는 파일을 막지 못한다. 데모 외부 이미지를 수정에 재사용하는 예외는 두지 않는다.
- **고아 파일은 즉시 삭제하지 않는다.** 이미지 교체 시 DB 연결은 orphanRemoval로 교체하지만, 파일은 다른 참조와 롤백을 고려해 유지한다. soft delete 역시 상품·파일을 보존한다. 유예기간과 참조 확인을 갖춘 정리 배치는 후속 작업이다.
- **끌어올리기의 현재 시간은 서비스에서 기존 Clock으로 계산해 엔티티에 전달한다.** API 등록 시 bumpedAt도 같은 Clock에서 받는다. 기존 builder 호출 호환용 기본값은 유지한다. 기존 직접 now() 비교를 바꾼 이유는 정확히 24시간인 경계를 허용하고 고정 시계로 테스트하기 위해서다. SOLD는 끌어올리기를 금지한다.
- **상품 쓰기와 구매확정은 상품 행 잠금을 공유한다.** 상품→거래 순서로 잠근 뒤 최신 상태를 검사한다. 동시에 들어온 끌어올리기나 상태 변경의 중복 처리를 막으며 스키마 버전 컬럼은 추가하지 않는다. `viewCount`·`favoriteCount`의 updatable=false는 그대로다.
- **판매자가 먼저 SOLD로 표시했더라도 구매확정은 허용한다.** 이전 Trade.confirm()은 markSold()가 이미 SOLD를 거부해 실패했다. 거래의 PAID/SHIPPING 검증은 유지하고, 이미 SOLD면 상품 전이만 생략한다. 상품 표시와 구매자 수령 확인을 분리하기 위한 변경이다.
- **판매내역은 createdAt DESC, id DESC 커서다.** 기존 Cursor/PageSize/CursorResponse와 ProductThumbnails를 재사용하고 삭제 상품은 제외한다. 상품 쓰기 응답은 상세 GET을 재호출하지 않아 조회수를 올리지 않는다.
- **2026-09-17 배포를 미루고 기능 개발을 먼저 한다.** Oracle Cloud 서버 생성·SSH 접속 과정이 복잡해 중단했다. 유료 호스팅은 쓰지 않는다. 배포 준비물(Dockerfile·CI·Compose)은 검증된 상태로 두고, 재개 시 대상만 정한다.
- **채팅하기는 상품 행 잠금으로 방 만들기를 한 줄로 세운다.** 찜처럼 UNIQUE 위반을 잡아 처리하지 않는 이유: 위반이 난 트랜잭션은 롤백 전용이 되어 같은 트랜잭션에서 기존 방을 다시 조회해 돌려줄 수 없다. 기존 방 조회도 잠금 조회로 한다. REPEATABLE READ 의 일반 SELECT 는 잠금을 기다리는 동안 먼저 커밋된 방을 못 볼 수 있다. UNIQUE `(product_id, buyer_id)`는 최종 방어선이다.
- **채팅방의 모든 기능은 "나가지 않은 참여자"만 쓰고, 아니면 404다.** 남의 방에 403을 주면 그 방이 있다는 사실이 드러난다(거래와 같은 규칙).
- **메시지 전송은 방 행을 잠근다.** 잠그지 않으면 거의 동시에 보낸 두 메시지의 커밋 순서가 뒤바뀌어 목록의 `last_message`가 실제 마지막 메시지와 달라질 수 있다.
- **채팅 응답은 요청자 기준(`opponent`, `myRole`)이다.** buyer/seller 를 그대로 주면 프론트가 매번 자기 id 와 비교해야 한다.
- **메시지 조회는 읽음 처리를 하지 않는다.** GET 에 부수효과를 두지 않고, 화면이 실제로 보일 때 `PATCH /read`를 따로 부른다. 안 읽은 수는 목록 한 페이지의 방 id 로 한 번에 센다(방마다 COUNT 를 보내지 않는다).
- **메시지 커서는 id 하나로 정렬하지만 형식은 `{id}_{id}`로 둔다.** 인덱스 `(room_id, id DESC)` 그대로 쓰고, API 전체의 커서 형식과 파서를 하나로 유지하기 위해서다.
- **메시지가 없는 방은 채팅 목록에서 뺀다.** "채팅하기"만 누르고 떠난 방이 판매자 목록에 쌓이지 않게 한다. `chat_count`는 방이 생길 때 오르므로 말없이 떠난 방도 센다.
- **직거래는 채팅방에서 한다: 판매자 예약 → 판매자 거래완료, 예약 취소는 양쪽.** 결제가 없어 서버가 대금을 맡지 않으므로 판매자 완료로 충분하다(당근마켓 방식). 구매자가 포기할 방법이 없으면 상품이 묶이므로 취소는 양쪽이 한다. 결제가 붙는 택배 거래는 PAID → SHIPPING → 구매자 구매확정 경로를 따로 쓴다.
- **`REQUESTED → CONFIRMED` 를 열면서 구매확정 가능 여부를 전이 가능 여부에서 분리했다(`TradeStatus.isBuyerConfirmable`).** `canTransitionTo(CONFIRMED)`로 계산하면 예약 단계에서 구매자 구매확정 버튼이 열린다. `Trade.confirm()`도 PAID·SHIPPING 만 허용한다.
- **진행 중 거래가 있으면 상품 상태 수동 변경·삭제를 409 로 막는다.** 거래는 예약인데 상품만 판매중이 되는 불일치를 막는다. 거래 상태는 채팅방에서만 바꾼다.
- **채팅방 응답에 `tradeActions`(누를 수 있는 버튼)를 서버가 계산해 준다.** canConfirm 과 같은 이유다. 잠금 순서는 상품 → 방 → 거래(구매확정·채팅하기와 같은 방향). 잠금 전에 상품 id 만 스칼라로 읽는다. 방 엔티티를 먼저 읽으면 뒤이은 잠금 조회가 영속성 컨텍스트의 낡은 상태를 돌려준다.
- **거래 변경은 시스템 메시지로 알린다.** 새 이벤트 종류를 만들지 않고 일반 메시지 경로(목록 미리보기·상대 목록 복귀·실시간 전달)를 그대로 탄다. 상대 화면은 SYSTEM 메시지를 받으면 방 정보를 다시 불러온다.
- **프로필 수정은 닉네임과 사진을 항상 함께 받는다.** PATCH 를 "보낸 필드만 변경"으로 두면 사진 삭제(null)와 변경 없음을 구분할 수 없다. 닉네임 중복은 먼저 확인하지만 동시 변경은 둘 다 확인을 통과할 수 있어, `uk_users_nickname` 위반을 flush 시점에 잡아 409 로 바꾼다. 바뀌는 UNIQUE 컬럼이 닉네임 하나라 가입과 달리 원인을 구분할 필요가 없다. 동시성 테스트는 두 요청이 중복 확인을 마친 지점에서 만나게 해(CyclicBarrier) 타이밍 운에 기대지 않는다.
- **정리 배치는 1,000건씩 별도 트랜잭션으로 지운다(`DataCleanupJob`).** 한 트랜잭션에서 대량 삭제하면 트랜잭션이 길어져 같은 테이블 쓰기(알림 저장 등)가 오래 기다린다. 지울 id 를 먼저 읽고 id 로 지워 MySQL 전용 `DELETE ... LIMIT` 없이 H2 테스트로 같은 코드를 검증한다. 검색 로그 id 조회는 정렬하지 않는다(ORDER BY id 가 범위 전체 filesort 를 일으켰다). 보관 기간: 검색 로그 7일(24시간치만 쓰지만 확인 여유), 읽은 알림 30일, 안 읽은 알림 90일(오래 접속하지 않은 사람도 돌아와 볼 여유).
- **예약 작업 전용 스케줄러를 둔다(`SchedulingConfig`).** 없으면 `@EnableScheduling` 이 WebSocket 브로커의 하트비트 스케줄러를 찾아 써 정리 작업과 하트비트가 스레드를 나눈다. 스케줄러를 빈으로 두지 않는 이유는 TaskScheduler 빈이 둘이 되면 WebSocket 설정의 주입이 모호해지기 때문이고, 대신 직접 종료한다. 테스트는 `app.scheduling.enabled=false` 로 끄고 정리 작업을 직접 실행한다. 설정값 등록(`CleanupProperties`)은 스케줄링을 꺼도 필요해 작업 클래스 쪽에 둔다.
- **검색 로그는 조회 트랜잭션이 끝난 뒤 전용 실행기에서 비동기로 저장한다(`SearchLogRecorder`, `AsyncConfig`).** 검색 응답이 로그를 기다리지 않는다. Spring 기본 실행기는 대기열 크기 제한이 없어 검색이 몰리면 밀린 작업이 메모리에 쌓이므로, 스레드 2개·대기열 1,000건으로 막고 넘치면 버린다(로그는 버려도 되는 부가 정보). "더 보기"까지 세면 순위가 부풀어 첫 페이지만 기록한다.
- **인기 검색어는 1분 메모리 캐시(`PopularKeywordService`)다.** 새 의존성 없이 volatile 스냅샷 + 갱신 잠금으로 만든다. 만료 순간 요청이 몰려도 집계는 한 번만 돈다(기다린 스레드는 방금 갱신된 결과를 쓴다). 동점은 검색어 순으로 순위를 고정한다. 캐시 테스트는 시계를 움직여 실제로 기다리지 않는다.
- **알림은 원래 동작이 커밋된 뒤 REQUIRES_NEW 트랜잭션에서 저장한다(`NotificationEventListener`).** 서비스는 `NotificationRequestedEvent` 만 발행한다. 트랜잭션 안에서 바로 저장하면 알림 저장 실패가 찜·거래완료까지 되돌리고, 롤백된 동작의 알림이 남을 수도 있다. 대가는 커밋 직후 장애 시 알림 유실이며, 알림은 부가 정보라 받아들였다. 문구는 발행하는 쪽이 트랜잭션 안에서 완성한다(커밋 뒤에는 지연 로딩 불가).
- **채팅 메시지는 알림을 만들지 않는다.** 메시지마다 알림이 쌓이면 알림함이 채팅으로 도배되고, 채팅 목록의 방별 안 읽은 수가 이미 있다.
- **알림 실시간 전달은 채팅과 같은 개인 큐(`WebSocketConfig.USER_QUEUE`)를 쓴다.** 구독 허용 목록을 늘리지 않는다. 이벤트는 `type: NOTIFICATION` 으로 구분한다.
- **알림 대상은 `target_url` 경로로 남긴다.** 스키마에 대상 id 컬럼이 없고, 프론트에 라우터가 없어 경로를 해석해 화면을 연다. 라우터를 도입하면 그대로 쓸 수 있다.
- **WebSocket 은 푸시만 하고 전송은 REST 다.** 검증·에러 형식·잠금이 REST 에 있고, 전송 경로가 둘이면 규칙을 두 번 구현해야 한다. 보낸 사람은 HTTP 상태로 성공·실패를 바로 안다.
- **방별 채널(`/topic/chat-rooms/{id}`) 대신 사용자별 개인 큐(`/user/queue/chat`)로 보낸다.** 방별 채널은 남의 방 번호로 구독하는 것을 막는 검사가 필요하고, 빠뜨리면 대화가 샌다. 개인 큐는 서버가 대상을 정하므로 그런 구멍이 구조적으로 없다. 구독은 이 주소 하나만, `SEND`는 전부 거부한다(브로커 목적지로 직접 보내 남의 큐에 가짜 메시지를 넣는 것을 막는다).
- **WebSocket 인증은 연결 요청이 아니라 STOMP CONNECT 에서 한다.** 브라우저 WebSocket 은 연결 요청에 헤더를 못 붙이고, URL 쿼리의 토큰은 접속 로그에 남는다. 세션 사용자 이름은 회원 id 문자열이다.
- **실시간 전달은 커밋 후(`@TransactionalEventListener`)에만 한다.** 서비스는 이벤트만 발행하고 WebSocket 을 모른다. 전달 실패는 기록만 하고 삼킨다. 이미 커밋된 REST 요청을 실패로 바꾸면 사용자가 재전송해 메시지가 중복된다.

왜 이렇게 했는지. 나중에 뒤집으려 할 때 먼저 읽을 것.

- **`products.status`와 `trades`를 분리했다.** 상품 상태는 화면에 표시하는 팻말이고, 거래는 영수증이다. 결제 실패·환불이 생기면 상품 상태만으로는 표현할 수 없다.
- **`payments`는 `trades`에 1:N이다.** 결제는 실패 후 재시도할 수 있고, 그 이력이 남아야 한다. `order_id` UNIQUE로 멱등성을 보장해 웹훅 중복 처리를 막는다.
- **`trades.active_product_id`는 생성컬럼이다.** `status`가 취소/환불이면 NULL이 되어 UNIQUE 제약에서 빠진다. 덕분에 "진행 중 거래는 상품당 1건"이 서비스 레이어 락 없이 DB에서 보장된다.
- **`view_count`, `favorite_count`, `chat_count`는 의도적 비정규화다.** 목록에서 상품마다 COUNT 쿼리가 나가는 것을 막기 위한 것.
- **목록은 커서 페이징이다.** 끌어올리기로 순서가 계속 바뀌어서 offset 방식은 중복·누락이 생긴다.
- **카테고리·동네 조회 3개는 배열 응답의 예외다.** 개별 명세의 배열 형식을 유지한다. 카테고리는 전체를 `sortOrder, id` 오름차순으로, 주소 검색은 일치하는 전체를 `id` 오름차순으로 반환한다. 상품 등 커서 목록은 기존 Cursor / PageSize / CursorResponse를 재사용한다.
- **근처 동네는 DB 범위 조회 + Java Haversine 계산으로 찾는다.** MySQL 전용 공간 함수 없이 H2에서도 같은 코드를 검증한다. 5km에서 시작해 4배씩 반경을 넓히고 실제 원 안에 10개가 확보되면 종료한다. 사각형 후보 수만으로 종료하면 더 가까운 바깥 동네를 놓칠 수 있다. 최종 반경은 지구 반원 거리이며 그때 전체 조회해 10개 미만도 처리한다. 날짜변경선은 두 범위로 나누고 극점은 모든 경도를 포함한다. 거리 동률은 id로 정렬한다. 마스터 데이터가 적거나 드물면 반복 조회·최종 전체 조회 비용을 감수한다.
- **동네 검색어는 앞뒤 공백 제거 후 최대 82자다.** 스키마의 시/도 20자 + 시/군/구 30자 + 동 30자 + 공백 2자를 기준으로 한다. 전체 주소 부분 일치이며 LIKE의 `%`, `_`, 이스케이프 문자 `!`를 문자 그대로 처리한다.
- **Region 생성 시각은 BaseCreatedTimeEntity로 관리한다.** JPA 저장 시 값을 채우며 컬럼 정의는 동일하므로 SQL 스키마를 변경하지 않았다. 마스터 엔티티 Category·Region은 setter 대신 생성용 정적 팩터리를 제공한다. 테스트는 SQL 초기 데이터에 의존하지 않고 필요한 데이터를 직접 저장한다.
- **상품 목록은 단일 연관 fetch + 이미지·찜 일괄 조회다.** JPA Criteria로 필요한 필터만 구성하고 `size+1`개를 가져온다. 이미지 컬렉션을 페이징 쿼리에 fetch join하면 상품 행이 늘거나 메모리 페이징이 발생할 수 있어 별도 IN 조회로 분리했다. 로그인 목록은 상품/이미지/찜 3회, 비로그인은 찜 조회를 생략한다. 이미지는 `sortOrder, id` 오름차순의 첫 항목을 썸네일로 사용한다.
- **상품 커서는 정렬 방향과 id를 함께 비교한다.** 최신순은 `bumpedAt DESC, id DESC`, 낮은가격순은 `price ASC, id ASC`. 공통 Cursor / PageSize / CursorResponse를 그대로 재사용한다. 표시 날짜는 초 단위지만 커서에는 DB에서 읽은 소수초까지 보존해 페이지 경계를 훼손하지 않는다. 커서 상품이 삭제돼도 커서 값으로 다음 페이지를 찾는다. 페이지 사이 가격·끌어올리기 값이 변경되는 경우까지 스냅샷 일관성을 보장하는 것은 아니다.
- **상품 조회수는 DB 원자적 UPDATE로만 증가시킨다.** `view_count = view_count + 1`로 동시 증가분 유실을 막는다. 비로그인·타인 조회마다 증가하고 본인은 제외한다. UPDATE 후 영속성 컨텍스트를 비우고 같은 트랜잭션에서 상세를 읽어 증가한 값을 응답한다. Product의 `viewCount`는 `updatable=false`로 일반 엔티티 UPDATE에서 제외하고 기존 메모리 증가 메서드는 제거했다. `updatedAt = updatedAt` 자기 대입으로 MySQL ON UPDATE가 조회 시 수정 시각을 바꾸지 않게 한다.
- **상품 필터의 없는 ID는 빈 목록, 없는 상세는 404다.** 목록은 전체 상태(판매중·예약·완료)를 포함하며 삭제만 제외한다. 삭제됐거나 없는 상품 상세는 동일한 `PRODUCT_NOT_FOUND`다. 선택 검색어는 공백 제거 후 최대 50자이고, 빈 값은 필터 생략이다. 제목 또는 본문 부분 일치이며 LIKE 특수문자를 이스케이프한다.
- **상품 관련 auditing과 오류 처리를 공통 코드로 통일했다.** ProductImage·Favorite도 BaseCreatedTimeEntity를 상속한다. Product의 기존 상태·끌어올리기 예외는 BusinessException(INVALID_STATE)으로 바꿨다. 컬럼 타입·이름·제약 변경은 없으므로 SQL 스키마는 유지했다.
- **조회수 동시성 테스트는 별도 H2 DB와 커밋된 준비 데이터를 사용한다.** 테스트 메서드의 롤백 트랜잭션에 데이터를 두면 작업 스레드가 볼 수 없으므로 TransactionTemplate로 먼저 커밋한다. 각 서비스 호출은 별도 트랜잭션으로 실행하고 테스트 컨텍스트 종료 시 전용 DB 테이블을 제거한다. 일반 서비스·API 테스트는 기존처럼 @Transactional로 격리한다.
- **동네 인증 API 3개는 갱신된 내 동네 목록 전체를 돌려준다.** 대표를 옮기면 두 행이 한 번에 바뀌므로, 부분 응답을 화면에서 조합하면 어긋나기 쉽다.
- **첫 동네는 자동으로 대표가 되고, 대표를 삭제하면 남은 동네가 승격된다.** "동네는 있는데 대표는 없음"이라는 상태를 만들지 않기 위해서다. 그 상태를 허용하면 화면이 매번 예외 처리를 해야 한다.
- **동네 인증은 브라우저 위치로만 한다.** 이름으로 골라 인증하게 하면 가보지 않은 동네를 등록할 수 있어 동네 인증이라는 장치가 의미를 잃는다. 둘러보기용 동네 선택(홈)과는 다른 목적이다.
- **이미지 형식은 파일 내용(시그니처)으로 판별한다.** 확장자와 Content-Type 은 클라이언트가 정하는 값이라 믿을 수 없다. 저장 파일명은 UUID 로 바꿔 경로 조작과 중복·한글 파일명 문제를 없앤다.
- **업로드 파일은 `/api` 아래(`/api/images/**`)로 서빙한다.** 프론트 개발 서버는 `/api` 만 프록시하므로, 다른 경로를 쓰면 프록시와 배포용 리버스 프록시 규칙을 하나씩 더 만들어야 한다.
- **이미지 업로드와 상품 등록을 분리했다.** 사진 선택 즉시 업로드해야 등록 버튼에서 기다리지 않는다.
- **백엔드는 Vercel이 아니라 별도 서버에 올린다.** Vercel은 Java 서버를 상시 띄울 수 없고, 업로드 파일이 남지 않으며, 다음 단계인 채팅의 WebSocket을 유지할 수 없다. 프론트만 Vercel에 두고 `/api/*`를 백엔드로 전달한다.
- **서버 한 대에 MySQL·앱·Caddy를 Docker Compose로 올린다.** 관리형 DB를 쓰지 않아 비용이 가장 적다. 가장 작은 무료 서버(메모리 1GB)에서도 돌도록 나눠 써야 해 JVM(힙·코드캐시·다이렉트버퍼·톰캣 스레드)과 MySQL(버퍼풀·performance_schema)을 명시적으로 줄이고 스왑 2GB를 둔다. 실측으로 앱 461MB → 290MB.
- **배포 서버는 AWS가 아니라 Oracle Cloud 상시 무료다(2026-09-17 변경).** 처음엔 AWS EC2로 준비했으나 사용자 명의의 AWS 무료 혜택이 이미 소진돼 월 비용이 생긴다. 상시 무료는 기간 제한이 없고 ARM 서버 메모리가 넉넉하다. Docker Compose 구성은 그대로 쓰고 안내서만 바꿨다. 대가는 무료 ARM 자원 부족 시 생성 실패, 저사용 서버 회수 가능성이다(백업으로 대응).
- **이미지를 x86·ARM 두 종류로 같은 태그에 올린다.** 무료 서버의 주력은 ARM이지만 자원이 없으면 x86 무료 서버로 대신해야 한다. Dockerfile 빌드 단계는 `--platform=$BUILDPLATFORM`으로 CI 러너에서 한 번만 돈다. 지정하지 않으면 ARM 이미지의 Gradle 빌드가 에뮬레이션으로 돌아 수십 분이 걸린다(로컬 실측: ARM 이미지 64초).
- **이미지는 GitHub Actions에서 빌드하고 서버는 내려받기만 한다.** 1GB 서버에서 Gradle 빌드는 메모리 부족으로 느리거나 실패한다. 테스트를 통과해야 이미지가 올라간다.
- **백엔드에도 HTTPS(Caddy + DuckDNS)를 둔다.** Vercel→서버 구간이 HTTP면 비밀번호·토큰이 평문으로 인터넷을 지난다. 채팅 WebSocket은 Vercel을 거치지 못해 브라우저가 백엔드에 직접 `wss://`로 붙어야 하므로 어차피 필요하다.
- **SQL 로그는 `local` 프로필에서만 켠다.** 바인딩 값에 이메일·비밀번호 해시·토큰 해시가 찍힌다. 기본(프로필 없음)을 조용하게 둬야 운영에서 설정을 빠뜨려도 개인정보가 로그에 남지 않는다. `bootRun`이 `local`을 켠다.
- **스키마 파일 맨 위에 `SET NAMES utf8mb4`를 둔다.** MySQL Docker 이미지의 초기화 스크립트는 파일을 latin1로 읽어 초기 데이터의 한글이 이중 인코딩돼 저장됐다(로컬 Docker 검증에서 발견). 로컬 DB는 다른 방식으로 적용해 드러나지 않았다.
- **MySQL·앱 포트는 외부에 열지 않는다.** Compose에서 Caddy의 80/443만 공개하고, 보안 그룹도 22(내 IP)·80·443만 연다.
- **refresh token은 JWT가 아닌 무작위 문자열이고, DB에는 SHA-256 해시만 저장한다.** 어차피 DB 조회로 검증하니 서명이 필요 없다. 원문을 저장하면 DB 유출 시 바로 로그인에 쓸 수 있다. BCrypt가 아닌 이유는 256비트 난수라 대입 공격이 불가능하고, 솔트가 있으면 해시로 조회(UNIQUE 인덱스)할 수 없어서다.
- **재발급 조회는 `SELECT ... FOR UPDATE`다.** 같은 refresh token으로 동시 재발급이 오면 둘 다 성공해 한쪽 토큰이 조용히 무효가 된다. 잠금으로 두 번째 요청이 명확히 실패하게 한다.
- **401을 `EXPIRED_TOKEN`과 `INVALID_TOKEN`으로 나눴다.** 프론트는 만료일 때만 재발급하고, 위조면 즉시 로그아웃한다.
- **로그아웃해도 access token은 최대 30분 유효하다.** 막으려면 요청마다 블랙리스트를 조회해야 해서 JWT의 무상태 이점이 사라진다. 만료를 짧게 두는 것으로 대응한다.
- **비밀번호는 ASCII 8~64자다.** BCrypt는 72바이트까지만 처리하는데, 한글이 섞이면 글자 수 제한만으로는 넘을 수 있다.
- **UNIQUE 제약은 엔티티에도 선언하고, 스키마 SQL과 대조하는 테스트로 강제한다.** `ddl-auto: validate`는 컬럼과 타입만 보고 UNIQUE는 검사하지 않는다. 엔티티에 빠지면 H2 테스트에만 제약이 없어 "중복이 막히는지" 검증하는 테스트가 통과해 버린다. 실제로 `favorites`에서 이 일이 있었다.
- **찜 수·조회수는 엔티티가 아니라 DB에서 증감한다.** 메모리에서 `++` 하면 동시 요청의 증가분이 유실된다. 해당 컬럼은 `updatable = false`로 두어 일반 엔티티 UPDATE가 덮어쓰지 못하게 막는다.
- **테스트는 H2, 운영은 MySQL.** 테스트가 로컬 DB에 의존하면 CI에서 깨진다. 대신 MySQL 전용 문법(생성컬럼, FULLTEXT ngram)은 H2에서 검증되지 않으므로, 스키마 일치는 실제 실행 시 `validate`로 확인한다.

## 리뷰 검증 기록 (2026-09-17)

- 기준 커밋 979548a 유지, 커밋·푸시 없음. SQL 스키마·Spring Boot 버전 변경 없음.
- 최종 gradlew.bat build 성공. 테스트 321개 → 354개, 실패 0·오류 0·건너뜀 0. 서비스 11개·API 16개·동시성 6개 추가.
- 보호 장치를 제거한 세 가지 실패 검증 뒤 원상복구했고 전체 빌드를 다시 통과했다.
- 실제 MySQL 8.0.44 validate 기동, 브라우저 데스크톱·모바일 상품 쓰기/후기 E2E 4개 성공. 후기 중복·서로 다른 거래 동시 수신은 실제 MySQL에서도 확인했다. 온도 상하한·로그인 경합·양방향 동시 평가는 H2 테스트로 확인했다.
- 새 환경변수·수동 SQL 적용은 필요 없다. 확인용 계정·거래·후기·업로드는 남으며 실계정 삭제 없음.

