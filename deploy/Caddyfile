# DOMAIN 에 적힌 주소로 HTTPS 인증서를 자동 발급·갱신한다(Let's Encrypt).
# 80 포트로 들어온 요청은 자동으로 443 으로 리다이렉트된다.
{$DOMAIN} {
	encode gzip

	# 백엔드는 컨테이너 네트워크 안의 app:8080. 외부에서 8080 으로 직접 접근할 수 없다.
	reverse_proxy app:8080
}
